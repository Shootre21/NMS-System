{
  "name": "nms-ips-monitor",
  "private": true,
  "version": "1.0.0",
  "description": "A web-based Network Management System (NMS) dashboard to monitor IPS availability.",
  "main": "index.js",
  "scripts": {
    "start": "serve dist -p 3000",
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --external:react --external:react-dom",
    "dev": "esbuild index.tsx --bundle --outfile=dist/index.js --external:react --external:react-dom --servedir=. --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "serve": "^14.2.3",
    "typescript": "^5.5.3"
  }
}
